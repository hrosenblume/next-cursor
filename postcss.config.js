/**
 * PostCSS Configuration
 * =====================
 * 
 * Standard PostCSS setup for Tailwind CSS.
 * No modifications needed unless adding custom PostCSS plugins.
 */

module.exports = {
  plugins: {
    tailwindcss: {},
    autoprefixer: {},
  },
}

